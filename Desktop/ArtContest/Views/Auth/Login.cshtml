<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ArtContest - Вход</title>
    <link rel="stylesheet" href="~/css/Auth.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>

    <div class="auth-card">
        <h1 class="auth-title">Вход в ArtContest</h1>

        @if (ViewBag.Error != null)
        {
            <div class="error-message">@ViewBag.Error</div>
        }

        <form class="auth-form" method="post" action="/Auth/Login" onsubmit="return validateLoginForm()">
            <div class="input-group">
                <input type="text" id="loginInput" name="login" placeholder="Логин" required minlength="3" maxlength="50">
            </div>

            <div class="input-group password-group">
                <input type="password" id="passwordInput" name="password" placeholder="Пароль" required minlength="6" maxlength="100">
            </div>

            <button type="submit" class="btn-submit">Войти</button>
        </form>

        <div class="auth-footer">
            <a href="/Auth/Register" class="auth-link">Нет аккаунта? Регистрация</a>
        </div>
    </div>

    <script src="~/js/auth-validation.js"></script>

</body>
</html>
